# Scripts

Helper utilities that are not part of the main runtime:

- `setup.py` - interactive setup helper for backend installation and Claude Desktop configuration.
- `test_connection.py` - database and environment verification script.
- `example_usage.py` - programmatic FastMCP example queries against the backend.

Core runtime entrypoints remain at the repo root and in `chatgpt/`.
